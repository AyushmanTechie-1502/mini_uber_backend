version: '3.8'

services:
  mysqldb:
    image: mysql:latest
    container_name: mysql-backend-db
    environment:
      MYSQL_ROOT_PASSWORD: password
      MYSQL_DATABASE: uberidb
    ports:
      - "3307:3306"
    volumes:
      - mysql_data:/var/lib/mysql

volumes:
  mysql_data: